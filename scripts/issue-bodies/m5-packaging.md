## Packaging

Epic from [docs/BACKLOG.md](../docs/BACKLOG.md) — M5.

Reference: [docs/DECISIONS.md](../docs/DECISIONS.md) — asset pipeline.

- [ ] Binary asset pack format (`.dat`-style; audio packs e.g. `.adp` / audio data pack — name TBD)
- [ ] Pack tool: CRUD entries inside packs (create, read, update, delete) — replaces hand-copy from `raw-assets/`
- [ ] Game loader reads `assets/` packs only (no `raw-assets/` at runtime)
- [ ] Windows installer/build packaging for distribution
- [ ] Minimal static download page (website)
