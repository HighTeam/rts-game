## Reconnect

Epic from [docs/BACKLOG.md](../docs/BACKLOG.md) — M2.

- [ ] On reconnect: client receives sim state snapshot + resumes live inputs; replay input log where needed
- [ ] Grace period / pause-on-disconnect policy (decide early — affects UI and netcode)
